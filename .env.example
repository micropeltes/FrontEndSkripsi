VITE_API_BASE=/api/v1
VITE_API_TIMEOUT_MS=10000
VITE_API_KEY=

# Optional. Leave empty to use the app server/reverse-proxy paths (/health and /api).
# Set to http://127.0.0.1:8000 only when the backend allows browser CORS from the frontend origin.
VITE_API_BASE_URL=
